---
import MainLayout from "../layouts/MainLayout.astro";
import Pill from "../components/Pill.astro";
import Link from "../components/Link.astro";
import Text from "../components/Text.astro";
import Heading from "../components/Heading.astro";
---

<MainLayout title="">
  <section class="min-h-screen grid place-items-center">
    <video
      autoplay
      loop
      muted
      playsinline
      class="h-auto"
    >
      <source src="/cloudanimation1.webm" type="video/webm" />
    </video>
  </section>

  <section class="aboutgrid grid grid-cols-12 gap-4">
    <div class="border-3 border-[var(--color-primary)] rounded-xl col-span-7 p-2">
      <Heading
        size="h2"
        font="header"
        stylingclasses="text-[var(--color-primary)]"
      >
        About
      </Heading>
      <Text
        teksttype="paragraph"
        stylingclasses="text-[var(--color-primary)]"
      >
        Jeg er multimediedesignstuderende på 3. semester med fokus på digital
        design, især UX/UI og grafisk design. Jeg brænder for at skabe kreative
        løsninger, der taler til målgruppen. I min praktik ønsker jeg at opnå
        indsigt i professionelle designprocesser og teamwork. Som kollega er jeg
        samarbejdsorienteret, kommunikativ og bidrager med kreativitet.
      </Text>
    </div>

    <div class="border-3 border-[var(--color-primary)] rounded-xl col-span-4 col-start-9">
      <img src="" alt="" />
    </div>
  </section>

  <section class="skillsgrid grid grid-cols-12 grid-rows-2 gap-4">
    <!-- Left, top (cols 1–7, row 1) -->
    <div class="col-span-7 row-span-1 border-3 border-[var(--color-primary)] rounded-xl p-2">
		<Heading
        htmlheading="h3"
        font="header"
        stylingclasses="text-[var(--color-primary)]"
      >
        Languages
      </Heading>
	  <div class="space-x-4 space-y-4">
		<Pill label="English - Native" />
		<Pill label="Danish - Fluent" />
		<Pill label="German - Understand" />
	  </div>
    </div>

    <!-- Left, bottom (cols 1–7, row 2) -->
    <div class="col-span-7 row-start-2 border-3 border-[var(--color-primary)] rounded-xl p-2">
		<Heading
        htmlheading="h3"
        font="header"
        stylingclasses="text-[var(--color-primary)]"
      >
        Education
      </Heading>
	  <div class="space-x-4 space-y-4">
		<Pill label="2024 - Current" />
	  </div>
	  <Text
	  teksttype="paragraph"
	  stylingclasses="text-[var(--color-primary)]"
	>
	  Multimediedesigner, EK - Københavns Erhvervsakademi
    </div>

    <!-- Right, tall (skip col 8; start at col 9, span 4 cols & both rows) -->
    <div class="col-span-4 col-start-9 row-span-2 border-3 border-[var(--color-primary)] rounded-xl p-2">
		<Heading
        htmlheading="h3"
        font="header"
        stylingclasses="text-[var(--color-primary)]"
      >
        Skills
      </Heading>
	  <div class="space-x-4 space-y-4">
		<Pill label="Photoshop" />
		<Pill label="Premiere Pro" />
		<Pill label="After Effects" />
		<Pill label="Illustrator" />
		<Pill label="Figma" />
		<Pill label="Light Room" />
		<Pill label="UX/UI" />
		<Pill label="Tailwind" />
		<Pill label="Astro" />
		<Pill label="HTML/CSS" />
		<Pill label="Alpine.js" />
	  </div>
    </div>
  </section>

  <section class="pastprojects"></section>
</MainLayout>

<script>
  // Her kan du skrive JavaScript, der skal køre på siden
  // Du kan også bruge Alpine.js eller Motion One til at lave interaktive elementer
  /* Stagger animation */
</script>