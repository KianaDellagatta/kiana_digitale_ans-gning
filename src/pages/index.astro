---
import MainLayout from "../layouts/MainLayout.astro";
import Pill from "../components/Pill.astro";
import Link from "../components/Link.astro";
import Text from "../components/Text.astro";
import Heading from "../components/Heading.astro";
---

<MainLayout title="">
  <div class="min-h-screen grid place-items-center">
    <video autoplay loop muted playsinline class="h-auto">
      <source src="/cloudanimation1.webm" type="video/webm" />
    </video>
  </div>

  <!-- About -->
  <section class=" grid grid-cols-1 lg:grid-cols-12 gap-4 m-8 lg:m-20">
    <!-- About card -->
    <div class="relative border-3 border-[var(--color-primary)] rounded-xl col-span-1 lg:col-span-7 p-6 lg:p-4">
      <!-- Butterfly #2 (flutters on hover, no glow) -->
      <div
        class=" hidden lg:block butterfly-flutter absolute cursor-pointer select-none top-[410px] left-[400px] w-26 "
        style="--rot:-6deg"
      >
        <img src="/butterfly2.svg" alt="" aria-hidden="true" class="butterfly-img block w-full" />
      </div>

      <Heading htmlheading="h2" font="header" stylingclasses="text-[var(--color-primary)]">About</Heading>
      <Text teksttype="paragraph" stylingclasses="text-[var(--color-primary)]">
        Jeg er multimediedesignstuderende på 3. semester med fokus på digital design, især UX/UI og grafisk design.
        Jeg brænder for at skabe kreative løsninger, der taler til målgruppen. I min praktik ønsker jeg at opnå indsigt
        i professionelle designprocesser og teamwork. Som kollega er jeg samarbejdsorienteret, kommunikativ og bidrager
        med kreativitet.
      </Text>
    </div>

    <!-- Image card -->
    <div class="relative border-3 border-[var(--color-primary)] rounded-xl col-span-1 lg:col-span-4 lg:col-start-9">
      <!-- Butterfly #1 (flies around on hover) -->
      <div
        class=" hidden lg:block butterfly-fly absolute cursor-pointer select-none top-[-20px] right-[-20px] w-24"
        style="--rot:6deg"
      >
        <img src="/butterfly1.svg" alt="" aria-hidden="true" class="butterfly-img block w-full" />
      </div>

      <img src="/Kiana.png" alt="Picture of Kiana" class="rounded-xl w-full h-full object-cover" />
    </div>
  </section>

  <!-- Skills grid -->
  <section class=" grid grid-cols-1 lg:grid-cols-12 lg:grid-rows-2 gap-4 m-8 lg:m-20">
    <!-- Languages -->
    <div class="relative col-span-1 lg:col-span-5 lg:row-span-1 border-3 border-[var(--color-primary)] rounded-xl p-6 lg:p-4">
      <img
        src="/flower.svg"
        alt=""
        aria-hidden="true"
        class="hidden lg:block absolute -top-6 -left-6 w-14 z-20 select-none flower-anim"
      />
      <Heading htmlheading="h3" font="header" stylingclasses="text-[var(--color-primary)]">Languages</Heading>
      <div class="space-x-4 space-y-4">
        <Pill label="English - Native" />
        <Pill label="Danish - Fluent" />
      </div>
    </div>

    <!-- Education -->
    <div class="relative col-span-1 lg:col-span-5 lg:row-start-2 border-3 border-[var(--color-primary)] rounded-xl p-6 lg:p-4">
      <div
        class="hidden lg:block absolute -bottom-10 right-10 w-16 z-20 cursor-pointer select-none butterfly-fly-big"
        style="--rot:-6deg"
        aria-hidden="true"
      >
        <img src="/butterfly3.svg" alt="" class="block w-full butterfly-img" />
      </div>

      <Heading htmlheading="h3" font="header" stylingclasses="text-[var(--color-primary)]">Education</Heading>
      <div class="space-x-4 space-y-4">
        <Pill label="2024 - Current" />
      </div>
      <Text teksttype="paragraph" stylingclasses="text-[var(--color-primary)]">
        Multimediedesigner, EK - Københavns Erhvervsakademi
      </Text>
    </div>

    <!-- Skills (tall, right) -->
    <div class="relative col-span-1 lg:col-span-7 lg:col-start-7 lg:row-span-2 border-3 border-[var(--color-primary)] rounded-xl p-6 lg:p-4">
      <div
        class="hidden lg:block absolute -top-14 -right-10 z-20 transition-transform duration-200 will-change-transform hover:scale-110 hover:[filter:drop-shadow(0_0_22px_var(--color-primary))]"
        aria-hidden="true"
      >
        <img src="/sun.svg" alt="" class="w-36 select-none pointer-events-none animate-sun" />
      </div>

      <Heading htmlheading="h3" font="header" stylingclasses="text-[var(--color-primary)]">Skills</Heading>
      <div class="space-x-4 space-y-4">
        <Pill label="Photoshop" />
        <Pill label="Premiere Pro" />
        <Pill label="Figma" />
        <Pill label="After Effects" />
        <Pill label="Illustrator" />
        <Pill label="Light Room" />
        <Pill label="UX/UI" />
        <Pill label="Tailwind" />
        <Pill label="Astro" />
        <Pill label="HTML/CSS" />
        <Pill label="Alpine.js" />
      </div>
    </div>
  </section>

  <!-- Past Projects -->
  <section class="pastprojects m-8 lg:m-20 relative">
	<Heading
	  htmlheading="h2"
	  font="header"
	  stylingclasses="text-[var(--color-primary)] underline"
	>
	  Past Projects
	</Heading>
  
	<!-- Logofolio block -->
	<section class="relative mt-6 border-3 border-[var(--color-primary)] rounded-xl p-6 lg:p-8">
	  <!-- Flower2 (top-right) -->
	  <img
		src="/flower2.svg"
		alt=""
		class="hidden lg:block absolute -top-6 -right-6 w-14 flower-anim"
		aria-hidden="true"
	  />
  
	  <!-- Butterfly4 (left side) -->
	  <div
		class="hidden lg:block absolute top-1/2 -left-10 w-16 butterfly-flutter"
		style="--rot:-4deg"
		aria-hidden="true"
	  >
		<img src="/butterfly4.svg" alt="" class="w-full butterfly-img" />
	  </div>
  
	  <Heading
		htmlheading="h3"
		font="header"
		stylingclasses="text-[var(--color-primary)] mb-4"
	  >
		Logofolio
	  </Heading>
	  <div class="grid grid-cols-1 justify-items-center gap-6 lg:grid-cols-4">
		<img src="/kulturnat_logo.svg" alt="Kulturnat Logo" class="w-full max-w-[220px] object-contain" />
		<img src="/ccc_logo.svg" alt="CCC Logo" class="w-full max-w-[220px] object-contain" />
		<img src="/thehungrypassport_logo.svg" alt="The Hungry Passport Logo" class="w-full max-w-[220px] object-contain" />
		<img src="/maksofilm_logo.svg" alt="Maksofilm Logo" class="w-full max-w-[220px] object-contain" />
	  </div>
	</section>
  
	<!-- Character Design block -->
	<section class="relative mt-6 border-3 border-[var(--color-primary)] rounded-xl p-6 lg:p-8">
	  <!-- Flower3 (bottom-left) -->
	  <img
		src="/flower3.svg"
		alt=""
		class="hidden lg:block absolute -bottom-8 -left-8 w-20 flower-anim"
		aria-hidden="true"
	  />
  
	  <!-- Butterfly5 (bottom-right) -->
	  <div
		class="hidden lg:block absolute -bottom-10 -right-10 w-20 butterfly-fly"
		style="--rot:8deg"
		aria-hidden="true"
	  >
		<img src="/butterfly5.svg" alt="" class="w-20 butterfly-img" />
	  </div>
  
	  <Heading
		htmlheading="h3"
		font="header"
		stylingclasses="text-[var(--color-primary)] mb-4"
	  >
		Character Design
	  </Heading>
	  <div class="grid grid-cols-1 justify-items-center gap-6 lg:grid-cols-2">
		<img src="/game_characters.svg" alt="Game Characters" class="w-full max-w-[520px] object-contain" />
		<img src="/instant_characters.svg" alt="Instant Characters" class="w-full max-w-[520px] object-contain" />
	  </div>
	</section>
  </section>
</MainLayout>

<script>
  // Optional animations or interactivity
</script>

<style>
/* --- Butterfly #1 (short fly-around) --- */
@keyframes flyAround {
  0%   { transform: translate(0,0) rotate(var(--rot,0)); }
  20%  { transform: translate(40px,-40px) rotate(calc(var(--rot,0) + 20deg)); }
  40%  { transform: translate(80px,-10px) rotate(calc(var(--rot,0) - 10deg)); }
  60%  { transform: translate(-20px,-50px) rotate(calc(var(--rot,0) + 15deg)); }
  80%  { transform: translate(-40px,-20px) rotate(calc(var(--rot,0) - 10deg)); }
  100% { transform: translate(0,0) rotate(var(--rot,0)); }
}
.butterfly-fly .butterfly-img { transition: transform 0.3s ease; }
.butterfly-fly:hover .butterfly-img { animation: flyAround 2.5s ease-in-out forwards; }

/* --- Butterfly #2 (flutter on hover—no glow) --- */
@keyframes flutter {
  0%   { transform: translateY(0) rotate(calc(var(--rot,0) - 3deg)) scale(1); }
  25%  { transform: translateY(-2px) rotate(calc(var(--rot,0) + 6deg)) scale(1.03); }
  50%  { transform: translateY(0) rotate(calc(var(--rot,0) - 2deg)) scale(1.01); }
  75%  { transform: translateY(-1px) rotate(calc(var(--rot,0) + 4deg)) scale(1.03); }
  100% { transform: translateY(0) rotate(var(--rot,0)) scale(1); }
}
.butterfly-flutter .butterfly-img { transition: transform 0.3s ease; }
.butterfly-flutter:hover .butterfly-img { animation: flutter 500ms ease-in-out infinite; }

/* --- Butterfly #3 (idle flutter + BIG flight on hover) --- */
@keyframes flutterIdle {
  0%   { transform: translateY(0) rotate(calc(var(--rot,0) - 2deg)) scale(1); }
  50%  { transform: translateY(-3px) rotate(calc(var(--rot,0) + 4deg)) scale(1.02); }
  100% { transform: translateY(0) rotate(var(--rot,0)) scale(1); }
}
.butterfly-fly-big .butterfly-img {
  animation: flutterIdle 2s ease-in-out infinite;
  transition: transform 0.3s ease;
}
@keyframes flyAroundBig {
  0%   { transform: translate(0,0) rotate(var(--rot,0)); }
  15%  { transform: translate(80px,-60px) rotate(calc(var(--rot,0) + 20deg)); }
  30%  { transform: translate(160px,-20px) rotate(calc(var(--rot,0) - 10deg)); }
  50%  { transform: translate(-60px,-100px) rotate(calc(var(--rot,0) + 25deg)); }
  70%  { transform: translate(-100px,-40px) rotate(calc(var(--rot,0) - 15deg)); }
  100% { transform: translate(0,0) rotate(var(--rot,0)); }
}
.butterfly-fly-big:hover .butterfly-img { animation: flyAroundBig 3.5s ease-in-out forwards; }

/* 🌸 Flower */
@keyframes sway {
  0%,100% { transform: rotate(-4deg) translateY(0); }
  50%     { transform: rotate(4deg) translateY(-2px); }
}
@keyframes spinBloom {
  0%   { transform: scale(1) rotate(0deg); }
  50%  { transform: scale(1.2) rotate(180deg); }
  100% { transform: scale(1) rotate(360deg); }
}
.flower-anim {
  animation: sway 5s ease-in-out infinite;
  transition: transform 0.3s ease, filter 0.3s ease;
}
.flower-anim:hover {
  animation: spinBloom 1.5s ease-in-out;
  filter: drop-shadow(0 0 18px hsla(223, 100%, 50%, 0.966));
}

/* --- Sun --- */
@keyframes spinBob {
  0%   { transform: translateY(0) rotate(0deg); }
  50%  { transform: translateY(-6px) rotate(180deg); }
  100% { transform: translateY(0) rotate(360deg); }
}
.animate-sun { animation: spinBob 18s linear infinite; }

/* Motion safety */
@media (prefers-reduced-motion: reduce) {
  .animate-sun, .butterfly-img, .flower-anim { animation: none !important; }
}

/* 🌸 Flowers idle sway */
@keyframes sway {
  0%,100% { transform: rotate(-4deg) translateY(0); }
  50%     { transform: rotate(4deg) translateY(-3px); }
}
.flower-anim {
  animation: sway 5s ease-in-out infinite;
  transition: transform 0.3s ease, filter 0.3s ease;
}
.flower-anim:hover {
  transform: scale(1.15);
  filter: drop-shadow(0 0 14px var(--color-primary));
}

/* 🦋 Butterfly flutter hover */
@keyframes flutter {
  0%   { transform: translateY(0) rotate(calc(var(--rot,0) - 3deg)) scale(1); }
  25%  { transform: translateY(-3px) rotate(calc(var(--rot,0) + 6deg)) scale(1.05); }
  50%  { transform: translateY(0) rotate(calc(var(--rot,0) - 2deg)) scale(1); }
  75%  { transform: translateY(-2px) rotate(calc(var(--rot,0) + 4deg)) scale(1.05); }
  100% { transform: translateY(0) rotate(var(--rot,0)) scale(1); }
}
.butterfly-flutter:hover .butterfly-img {
  animation: flutter 600ms ease-in-out infinite;
}

/* 🦋 Butterfly fly-around (larger path) */
@keyframes flyAround {
  0%   { transform: translate(0,0) rotate(var(--rot,0)); }
  20%  { transform: translate(60px,-50px) rotate(calc(var(--rot,0) + 20deg)); }
  40%  { transform: translate(120px,-10px) rotate(calc(var(--rot,0) - 10deg)); }
  60%  { transform: translate(-30px,-80px) rotate(calc(var(--rot,0) + 25deg)); }
  80%  { transform: translate(-60px,-20px) rotate(calc(var(--rot,0) - 15deg)); }
  100% { transform: translate(0,0) rotate(var(--rot,0)); }
}
.butterfly-fly:hover .butterfly-img {
  animation: flyAround 3s ease-in-out forwards;
}

/* 🛡️ Motion safety */
@media (prefers-reduced-motion: reduce) {
  .flower-anim,
  .butterfly-img { animation: none !important; }
}

</style>